# Scripts

This directory will hold the future local runtime helpers.

Planned command groups:

```text
memory/
  init target memory
  ingest sources
  record decisions
  query duplicates
  export markdown

labs/
  create realistic PoC workspaces
  validate configuration legitimacy
  capture negative controls
```

